import { DashboardOrderByPipePipe } from './dashboard-order-by-pipe.pipe';

describe('DashboardOrderByPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DashboardOrderByPipePipe();
    expect(pipe).toBeTruthy();
  });
});
