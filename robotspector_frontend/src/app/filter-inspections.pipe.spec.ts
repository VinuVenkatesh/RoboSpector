import { FilterInspectionsPipe } from './filter-inspections.pipe';

describe('FilterInspectionsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterInspectionsPipe();
    expect(pipe).toBeTruthy();
  });
});
